# Bounded context for product sourcing audits (e.g. merchant status checks).
# ".": depends on the shared root package (Catalog models, ActiveRecord).
# "lib/google_merchant": the merchant integration adapter.

enforce_dependencies: true

dependencies:
  - "."
  - "lib/google_merchant"